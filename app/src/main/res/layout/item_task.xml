<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView
        xmlns:android="http://schemas.android.com/apk/res/android"
        xmlns:app="http://schemas.android.com/apk/res-auto"
        xmlns:tools="http://schemas.android.com/tools"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="@dimen/padding_medium"
        app:cardCornerRadius="@dimen/corner_normal"
        app:cardElevation="@dimen/elevation_small"
        app:cardUseCompatPadding="true"
        android:id="@+id/cv_taskItem">

    <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:padding="@dimen/padding_regular"
            android:id="@+id/cl_taskItemContainer">

        <ImageView
                android:id="@+id/iv_taskStatusImage"
                android:layout_width="15dp"
                android:layout_height="15dp"
                app:layout_constraintStart_toStartOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                tools:src="@drawable/ic_new_status"
                tools:ignore="ContentDescription" />

        <TextView
                android:id="@+id/tv_taskName"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_regular"
                android:textColor="@color/black"
                android:layout_marginLeft="@dimen/margin_normal"
                android:layout_marginStart="@dimen/margin_normal"
                app:layout_constraintStart_toEndOf="@id/iv_taskStatusImage"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="On-site Verification" />

        <TextView
                android:id="@+id/tv_taskDue"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_normal"
                android:layout_marginLeft="@dimen/margin_normal"
                android:layout_marginStart="@dimen/margin_normal"
                android:layout_marginTop="@dimen/margin_small"
                android:drawableEnd="@drawable/ic_edit"
                android:drawableRight="@drawable/ic_edit"
                android:drawablePadding="@dimen/padding_normal"
                app:layout_constraintStart_toEndOf="@id/iv_taskStatusImage"
                app:layout_constraintTop_toBottomOf="@id/tv_taskName"
                tools:text="5 Nov, 2019 : 4-5 PM" />

        <TextView
                android:id="@+id/tv_taskStatusText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:textSize="@dimen/text_normal"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Submitted"
                tools:textColor="#00DB00"/>

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>